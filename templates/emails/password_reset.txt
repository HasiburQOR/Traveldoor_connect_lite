Hello {{ user.first_name|default:user.username }},

We received a request to reset the password for the {{ company_name }} Connect
account {{ email }}.

Open the link below to choose a new password:

{{ base_url }}{{ reset_path }}

The link works once and expires in {{ reset_timeout_hours }} hours. If you did
not request this, ignore the email — the current password stays unchanged.

Sign in instead: {{ base_url }}{{ login_path }}

This is an automated message — please do not reply.
